import { AbstractComponent, InferContext } from "./abstract";

export class Parameters extends AbstractComponent {
    constructor(context: InferContext<Parameters>) {
        super(context);
      }
}